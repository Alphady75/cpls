{% extends 'admin.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>Ulr</th>
                <td>{{ video.ulr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_videos_index') }}">back to list</a>

    <a href="{{ path('admin_videos_edit', {'id': video.id}) }}">edit</a>

    {{ include('admin/videos/_delete_form.html.twig') }}
{% endblock %}
