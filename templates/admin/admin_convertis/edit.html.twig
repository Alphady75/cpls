{% extends 'admin.html.twig' %}

{% block title %}Edit Convertis{% endblock %}

{% block body %}
    <div class="card">
    <div class="card-body">
        <div class="row mb-4">
            <div class="col-sm-6">
                <h2 class="card-title mb-0">Edition des information du convertis</h2>
            </div>
            <div class="col-sm-6 text-right">
                {{ include('admin/admin_convertis/_delete_form.html.twig') }}
            </div>
        </div>

        {{ include('admin/admin_convertis/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        
    </div>
</div>
{% endblock %}
